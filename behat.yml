default:

  suites:

    default:
      paths: [ %paths.base%/tests/behat/feature_files/ ]
      filters: { tags: '~@wip' }
      contexts:
        - \Slim\Behat\Context\FeatureContext:
          - "@app"
        - \Slim\Behat\Context\BasicSmokeTestContext:
          - "@app"
        - \Slim\Behat\Context\MiddlewaresContext:
          - "@app"

      services:
        app:
          class: "Slim\\App"
